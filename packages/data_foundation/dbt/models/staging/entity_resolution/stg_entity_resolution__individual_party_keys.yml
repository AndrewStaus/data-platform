# Documentation for the identity stitching staging model.
models:
  - name: stg_entity_resolution__individual_party_keys
    columns:
      - name: individual_party_key
        data_type: varchar
        description: "{{ doc('individual_party_key') }}"
      - name: individual_id
        data_type: varchar
        description: "{{ doc('individual_id') }}"
      - name: updated_at
        data_type: timestamp_ntz
        description: When the relationship changed
      - name: _loaded_at
        data_type: timestamp_tz
        description: When the record was ingested
    description: Mappings of individual party keys to individual entities.  This is used to identify unique users across multiple platforms
