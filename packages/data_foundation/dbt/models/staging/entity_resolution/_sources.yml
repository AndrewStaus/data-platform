# Source definition for identity stitching keys.
version: 2

sources:
  - name: entity_resolution
    database: "{{ '_'+target.name+'_' if not target.name == ('prod') -}} raw"
    schema: "entity_resolution {{- '__'+target.user if not target.name == ('prod') }}"
    # database: "{{ generate_database_name('raw') }}"
    # schema: "{{ generate_schema_name('entity_resolution') }}"
    tables:
      - name: individual_party_keys