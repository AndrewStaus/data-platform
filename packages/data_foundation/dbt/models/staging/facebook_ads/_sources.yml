# Source definition for the facebook_ads schema replicated by Sling.
version: 2

sources:
  - name: facebook_ads
    database: "{{ '_'+target.name+'_' if not target.name == ('prod') -}} raw"
    schema: facebook_ads {{- '__'+target.user if not target.name == ('prod') }}
    # database: "{{ generate_database_name('raw') }}"
    # schema: "{{ generate_schema_name('facebook_ads') }}"
    tables:
      - name: campaigns
        columns:
          - name: ID
            description: ''
            config:
              meta: {}
              tags: []
          - name: NAME
            description: ''
            config:
              meta: {}
              tags: []
          - name: START_DATE
            description: ''
            config:
              meta: {}
              tags: []
          - name: UPDATED
            description: ''
            config:
              meta: {}
              tags: []
          - name: _DLT_LOAD_ID
            description: ''
            config:
              meta: {}
              tags: []
          - name: _DLT_ID
            description: ''
            config:
              meta: {}
              tags: []
